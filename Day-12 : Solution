Minimize the Heights II
Solution:
class Solution:
    def getMinDiff(self, arr, k):
        n = len(arr)
        arr.sort()

        # Initial difference
        ans = arr[-1] - arr[0]

        # Smallest and largest after considering extremes
        small = arr[0] + k
        big = arr[-1] - k

        if small > big:
            small, big = big, small

        for i in range(n - 1):
            min_height = min(small, arr[i+1] - k)
            max_height = max(big, arr[i] + k)

            if min_height < 0:
                continue  # invalid because heights can't be negative

            ans = min(ans, max_height - min_height)

        return ans
