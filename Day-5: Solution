2749. Minimum Operations to Make the Integer Zero
Solution:
class Solution:
    def makeTheIntegerZero(self, num1: int, num2: int) -> int:
        for k in range(1, 61):  # try up to 60 operations
            target = num1 - k * num2

            # Condition 1: target must be positive
            if target <= 0:
                continue

            # Condition 2 & 3: check set bits and minimum possible sum
            if bin(target).count("1") <= k <= target:
                return k
        
        return -1
